import { StrategyView } from "@/components/strategy/strategy-view";

export const metadata = { title: "Strategy Room · Emory" };

export default function StrategyPage() {
  return <StrategyView />;
}
