import { IntegrationsView } from "@/components/integrations/integrations-view";

export const metadata = { title: "Wire Room · Emory" };

export default function IntegrationsPage() {
  return <IntegrationsView />;
}
