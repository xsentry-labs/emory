import { CustomersView } from "@/components/customers/customers-view";

export const metadata = { title: "Customers · Emory" };

export default function CustomersPage() {
  return <CustomersView />;
}
